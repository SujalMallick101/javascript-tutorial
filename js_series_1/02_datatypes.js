//A data type in JavaScript (or in any programming language) specifies the kind of data a variable can hold.

let name="sujal"//String-sequence of characters
let age=21//Numbers-Used for both integer and floating-point numbers
let isLoggedIn=true//Boolean-Represents true or false
let x;
console.log(x); // undefined-A variable that has been declared but not assigned a value is undefined.
const bigNum = 1234567890123456789012345678901234567890n//Bigint-Used for integers larger than 2^53 - 1


//typeOf function to check type of a variable
console.log(typeof(name));//string
console.log(typeof(age));//number




